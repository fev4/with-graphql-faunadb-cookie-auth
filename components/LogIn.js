import React from 'react';
const LOGIN_USER = `
    mutation loginUser($data: LoginUserInput!) {
      loginUser(data: $data) {
        userId
      loginUser(data: $data) {
        userId
        userToken
      }
        userId
        userToken
      }
        userId
        userToken
      }
      }
    }
  `;
